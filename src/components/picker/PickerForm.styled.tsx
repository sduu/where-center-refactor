import styled from 'styled-components';
import { GAP } from '../../constants/styles.constant';

export const PickerFormWrapper = styled.form`
  display: flex;
  flex-direction: column;
  gap: ${GAP.sm};
`;
