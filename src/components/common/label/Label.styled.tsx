import styled from 'styled-components';
import { COLOR, FONTSIZE } from '../../../constants/styles.constant';

export const LabelWrapper = styled.label`
  color: ${COLOR.textMain};
  font-size: ${FONTSIZE.base};
`;
